# Solo-maintainer governance TDD marker

This temporary governance-only marker records that the ruleset contract test was committed before the desired-state implementation. It will be removed after the red GitHub Actions result is observed.
