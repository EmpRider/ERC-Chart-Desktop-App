// Maintained indicator sources in this workspace are inputs to the ERC
// authoring compiler. They must not be imported as ordinary JavaScript modules
// before that transform has lowered Pine-style globals and script statements.
export {};
